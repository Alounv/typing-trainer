/**
 * `stores` lib — public API surface.
 *
 * Theme-picker Svelte 5 rune store. The `.svelte.ts` file is barrel-safe
 * (it's a TypeScript module, not a component).
 */
export * from './theme.svelte';
