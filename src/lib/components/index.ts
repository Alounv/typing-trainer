/**
 * `components` lib — public API surface.
 *
 * Shared UI components that aren't owned by a specific domain lib.
 * Re-exported as named exports so callers don't need to know each
 * component's individual file path.
 */
export { default as ThemeSelect } from './ThemeSelect.svelte';
